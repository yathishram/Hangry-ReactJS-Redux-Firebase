import React from "react";

const SearchInstructions = ({ instruction }) => {
  return (
    <div className="container">
      <p>{instruction}</p>
    </div>
  );
};

export default SearchInstructions;
